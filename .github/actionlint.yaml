# Custom labels for self-hosted runners, so actionlint doesn't flag them as
# unknown. `s89` is the runner on the Tencent Cloud (Beijing) box that runs the
# deploy job in deploy.yml. See docs/deploy-s89.md.
self-hosted-runner:
  labels:
    - s89
