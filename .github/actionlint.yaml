# Custom labels for self-hosted runners, so actionlint doesn't flag them as
# unknown. `aliyun` is the Aliyun deploy host for deploy.yml (dev). `s89` is
# retained so older workflow revisions still lint cleanly during cutover.
self-hosted-runner:
  labels:
    - aliyun
    - s89
