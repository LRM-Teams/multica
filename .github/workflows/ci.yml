name: CI

on:
  pull_request:
    branches: [dev]
  # Warms the shared Turbo cache. A cache written by a pull request is scoped to
  # refs/pull/<n>/merge and can only be restored by re-runs of that same pull
  # request; the only scope every PR can read is the base branch. With
  # pull_request as the sole trigger nothing ever wrote a current dev-scoped
  # entry, so every PR restored a stale one (the live entry dated from
  # 2026-07-28) and turbo reported 0 of 9 tasks cached. Tests are skipped on
  # this trigger — see the frontend-test job's `if`.
  push:
    branches: [dev]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  # PR: compare to origin/dev. Push to dev: compare to the previous HEAD.
  # All-zero before SHA (new branch) is handled in scripts/ci-pr-scope.sh.
  BASE_REF: ${{ github.event_name == 'pull_request' && 'origin/dev' || github.event.before }}
  GITHUB_EVENT_BEFORE: ${{ github.event.before }}

jobs:
  frontend:
    # GitHub-hosted. The self-hosted CI pool was retired on 2026-07-29; the only
    # self-hosted selector left in the repo is deploy.yml's [self-hosted, aliyun],
    # which is the production deploy runner. Standard runners are 2 vCPU / 8 GB
    # on private repos — see the "Report runner size" step below for the
    # measured values.
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Fetch origin/dev
        if: github.event_name == 'pull_request'
        run: git fetch --no-tags origin dev:refs/remotes/origin/dev

      - name: Resolve PR scope
        id: scope
        run: bash scripts/ci-pr-scope.sh >> "$GITHUB_OUTPUT"

      - name: Prove the scope classifier
        # Always. This script is the merge-gate classifier; if it lies, every
        # later skip/full decision is wrong. It does not need Node or Go.
        run: bash scripts/ci-pr-scope.test.sh

      - name: Test self-host env derivation
        if: steps.scope.outputs.run_selfhost == 'true'
        run: bash scripts/selfhost-config.test.sh

      - name: Enforce web-only workflow policy
        if: steps.scope.outputs.run_web_workflows == 'true'
        run: bash scripts/web-workflows.test.sh

      - name: Check baked web public-origin gate
        if: steps.scope.outputs.run_baked_origins == 'true'
        run: bash scripts/assert-baked-web-public-origins.test.sh

      - name: Setup pnpm
        if: steps.scope.outputs.frontend_mode != 'skip' || steps.scope.outputs.run_reserved_slugs == 'true'
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        if: steps.scope.outputs.frontend_mode != 'skip' || steps.scope.outputs.run_reserved_slugs == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        if: steps.scope.outputs.frontend_mode != 'skip' || steps.scope.outputs.run_reserved_slugs == 'true'
        run: pnpm install

      - name: Restore Turbo cache
        if: steps.scope.outputs.frontend_mode != 'skip'
        uses: actions/cache@v4
        with:
          path: .turbo/cache
          key: ${{ runner.os }}-turbo-${{ github.workflow }}-${{ github.ref_name }}-${{ github.sha }}
          # These keys are own-first ON PURPOSE. Measured 2026-07-30; three of us
          # spent two days on it, so the answer lives here rather than in chat.
          #
          # 1. Matching is scope-outer / keys-inner. A run searches its OWN cache
          #    scope against every restore-key first, and returns on the first
          #    match there — it only falls through to the base-branch scope when
          #    NO key can match anything in its own scope.
          # 2. So once a PR has saved one entry, the broad `-turbo-CI-` prefix
          #    matches it and dev's entry is never consulted. That is inherent to
          #    the mechanism, not a misconfiguration.
          # 3. We keep it. An iterating PR's previous head is usually closer to
          #    its current head than dev is: measured `Cached: 8 of 9` between
          #    consecutive commits, versus `0 of 9` on a PR whose heads were
          #    large rewrites. The bad case costs one round and self-heals.
          # 4. Switching to dev-first requires deleting the broad prefix too.
          #    Removing only the per-ref prefix changes NOTHING — tried it in
          #    PR #1448, behaviour was identical, because the broad prefix still
          #    matched the PR's own entry.
          #
          # The exact `key` carries github.sha, so same-commit re-runs hit it
          # directly regardless of any of the above.
          restore-keys: |
            ${{ runner.os }}-turbo-${{ github.workflow }}-${{ github.ref_name }}-
            ${{ runner.os }}-turbo-${{ github.workflow }}-
            ${{ runner.os }}-turbo-

      - name: Restore Next.js cache
        if: steps.scope.outputs.frontend_mode != 'skip'
        uses: actions/cache@v4
        with:
          path: apps/web/.next/cache
          key: ${{ runner.os }}-next-${{ hashFiles('pnpm-lock.yaml') }}-${{ hashFiles('apps/web/**', 'packages/core/**', 'packages/ui/**', 'packages/views/**') }}
          restore-keys: |
            ${{ runner.os }}-next-${{ hashFiles('pnpm-lock.yaml') }}-
            ${{ runner.os }}-next-

      - name: Verify reserved-slugs.ts is up to date
        # Re-runs the generator and fails on any drift from the
        # checked-in TypeScript output. The Go side embeds the JSON
        # source directly, so a passing diff here proves both sides
        # share one source of truth.
        if: steps.scope.outputs.run_reserved_slugs == 'true'
        run: |
          pnpm generate:reserved-slugs
          git diff --exit-code -- packages/core/paths/reserved-slugs.ts

      - name: Build, type check, lint
        # Web-only (Frank 2026-07-28): drop desktop/docs/mobile from the PR
        # frontend job so CI stays on the product surface that still ships.
        # Affected mode uses ...[BASE_REF] plus explicit exclusions; full mode
        # keeps @multica/web... . Per-surface reasons below — deliberately not
        # one shared paragraph (task #12, 2026-08-01):
        #
        # - apps/desktop packaging: out of the main release gate on purpose
        #   (#1320) — desktop isn't shipped through this pipeline.
        # - apps/desktop typecheck: no CI coverage, no assigned owner. Found
        #   broken by accident once (2026-08-01, honor-tab.tsx) — that
        #   incident is the reason this line exists instead of silence. If
        #   you touch apps/desktop, run its typecheck yourself; CI won't.
        # - apps/mobile: no one maintains it (Frank, 2026-08-01) — not in any
        #   CI gate, and not going to be. If it's red, nobody is watching
        #   and nobody will fix it. Known and unfixed as of this line: 3
        #   type errors in data/schemas.ts (Agent/RuntimeDevice zod schemas
        #   drifted from the current types, including one reference to the
        #   removed Agent.visibility field) — whoever picks mobile back up
        #   starts there, not from a clean slate.
        # - apps/docs: no CI coverage, no assigned owner, low blast radius
        #   (infrequent changes). If you touch apps/docs, run its typecheck
        #   yourself; CI won't.
        if: steps.scope.outputs.frontend_mode != 'skip'
        run: bash scripts/ci-turbo-web.sh "${{ steps.scope.outputs.frontend_mode }}" build typecheck lint

  frontend-test:
    # The test half of the old single frontend job, split out (2026-08-06) so
    # build/typecheck/lint and the test suite run on two runners in parallel:
    # wall time becomes max(build job, test job) instead of their sum. This is
    # NOT the rejected test-sharding — no suite is split, and the split landed
    # only after the suite itself was cut ~40% (happy-dom + threads, #2394),
    # so it is not "adding runners without reducing work".
    #
    # PR-only, mirroring the old Test step's `if`: the dev push exists only to
    # publish a base-branch Turbo cache for build/typecheck/lint, and
    # re-running the suite after a merge produces no information the pull
    # request did not already give.
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Fetch origin/dev
        run: git fetch --no-tags origin dev:refs/remotes/origin/dev

      - name: Resolve PR scope
        id: scope
        run: bash scripts/ci-pr-scope.sh >> "$GITHUB_OUTPUT"

      - name: Setup pnpm
        if: steps.scope.outputs.frontend_mode != 'skip'
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        if: steps.scope.outputs.frontend_mode != 'skip'
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        if: steps.scope.outputs.frontend_mode != 'skip'
        run: pnpm install

      - name: Restore Turbo cache
        # Same restore-keys as the frontend job (the key-matching essay lives
        # there — read it before changing these). The exact key gets a `-test`
        # suffix so both jobs can save their own entry for the same sha; the
        # shared prefixes mean this job still restores whatever the build job
        # or dev push saved, which is what makes the `test` task's
        # `dependsOn: ^typecheck` usually a cache hit instead of a re-run.
        if: steps.scope.outputs.frontend_mode != 'skip'
        uses: actions/cache@v4
        with:
          path: .turbo/cache
          key: ${{ runner.os }}-turbo-${{ github.workflow }}-${{ github.ref_name }}-${{ github.sha }}-test
          restore-keys: |
            ${{ runner.os }}-turbo-${{ github.workflow }}-${{ github.ref_name }}-
            ${{ runner.os }}-turbo-${{ github.workflow }}-
            ${{ runner.os }}-turbo-

      - name: Report runner size
        # One line, so the runner's core count is a measurement in the log
        # rather than an assumption about GitHub's published spec. Vitest sizes
        # its worker pool from this number (see packages/views/vitest.config.ts).
        if: steps.scope.outputs.frontend_mode != 'skip'
        run: |
          echo "nproc=$(nproc) availableParallelism=$(node -p 'require("node:os").availableParallelism()')"
          echo "mem=$(free -g | awk '/^Mem:/{print $2}')GiB"

      - name: Test
        # `test` is a separate turbo invocation pinned to one outer task at a
        # time (#853/#855). Previously all four tasks shared one command, so
        # turbo ran several packages' `test` concurrently and each spawned its
        # own pool of Vitest file workers: outer concurrency x inner workers.
        # That oversubscribed the runner and starved whichever test was mid-
        # `waitFor`, reddening PRs whose diffs never touched views (#1243,
        # #1232). `--concurrency=1` bounds only the OUTER task graph; each
        # package keeps its own file workers, so views' files stay parallel.
        #
        # Deliberately NOT done here: raising any timeout. That would hide the
        # symptom and turn a stable red into an intermittent one.
        if: steps.scope.outputs.frontend_mode != 'skip'
        env:
          CONCURRENCY: "1"
        run: bash scripts/ci-turbo-web.sh "${{ steps.scope.outputs.frontend_mode }}" test

      - name: Skip unaffected frontend tests
        if: steps.scope.outputs.frontend_mode == 'skip'
        run: echo "No web-surface packages in this PR; frontend tests skipped"

  backend:
    # Also runs on the dev push (unlike the frontend-test job above, which
    # stays PR-only). Push scope is the merge's own diff against
    # github.event.before — not a second full-repo replay.
    # GitHub-hosted, same as frontend. Requires Docker for Postgres/Redis
    # service containers — see docs/ci-tencent-runner.md.
    runs-on: ubuntu-latest
    timeout-minutes: 45
    services:
      postgres:
        image: pgvector/pgvector:pg17
        env:
          POSTGRES_DB: multica
          POSTGRES_USER: multica
          POSTGRES_PASSWORD: multica
        # High host ports avoid colliding with Compose Postgres on self-hosted runners.
        ports:
          - 55432:5432
        options: >-
          --health-cmd "pg_isready -U multica -d multica"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
      redis:
        image: redis:7-alpine
        ports:
          - 56379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgres://multica:multica@localhost:55432/multica?sslmode=disable
      # Wires up internal/researchrun's real-PostgreSQL suite (55 guarded tests
      # across 14 files). Without this the whole Research Run persistence
      # evidence base — state machine triggers, dispatch outbox, attempt leases,
      # reconcile fencing, execution circuits, target repairs — skipped in CI and
      # only ever ran on a developer's machine.
      #
      # It gets its own database on purpose: the suite replays migration
      # down/up, so a mid-test rollback must not leave the shared schema broken
      # for the packages that run after it. Two recorded incidents came from
      # `migrate down` reaching a shared database.
      TEST_DATABASE_URL: postgres://multica:multica@localhost:55432/multica_research?sslmode=disable
      # Wires up the RedisLocalSkill*_test.go suite. Distinct from REDIS_URL
      # (which would flip the server binary itself onto the Redis-backed
      # realtime relay + request stores); the tests talk to this Redis
      # directly so they run alongside the Postgres-backed suite.
      REDIS_TEST_URL: redis://localhost:56379/1
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Fetch origin/dev
        if: github.event_name == 'pull_request'
        run: git fetch --no-tags origin dev:refs/remotes/origin/dev

      - name: Resolve PR scope
        id: scope
        run: bash scripts/ci-pr-scope.sh >> "$GITHUB_OUTPUT"

      - name: Setup Go
        if: steps.scope.outputs.go_mode != 'skip' || steps.scope.outputs.run_migration_numbers == 'true'
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.1"
          cache-dependency-path: server/go.sum

      - name: Expand Go packages
        id: go_pkgs
        if: steps.scope.outputs.go_mode != 'skip'
        env:
          GO_MODE: ${{ steps.scope.outputs.go_mode }}
          GO_SEED_PACKAGES: ${{ steps.scope.outputs.go_seed_packages }}
        run: |
          pkgs="$(bash scripts/ci-expand-go-packages.sh)"
          echo "packages=$pkgs" >> "$GITHUB_OUTPUT"
          echo "Affected Go packages: $pkgs"
          bash scripts/ci-expand-go-packages.test.sh

      - name: Reject duplicate migration version numbers
        # Incremental only. `dev` already carries 59 duplicated numbers, and the
        # migrator records `version` as the full filename, so renaming an applied
        # migration would make production treat it as never run — existing
        # duplicates must stay. See scripts/migration-numbers.test.sh.
        if: steps.scope.outputs.run_migration_numbers == 'true'
        run: bash scripts/migration-numbers.test.sh

      - name: Build
        if: steps.scope.outputs.go_mode != 'skip'
        run: cd server && go build ${{ steps.go_pkgs.outputs.packages }}

      # task #85 P0: catch unkeyed composite literals that silently mis-assign
      # when models.go field order changes (the silent-shift class next to
      # hand-written SQL drift). composites is on by default in go vet.
      - name: Go vet (composites + default checks)
        if: steps.scope.outputs.go_mode != 'skip'
        run: cd server && go vet ${{ steps.go_pkgs.outputs.packages }}

      - name: Cursor-deadlock check
        # Task #91: catches the #1803/#1812 shape (a rows cursor from
        # Query() left open while a nested call acquires a second pool
        # connection — deterministic pool deadlock under load). See
        # server/tools/cursordeadlock for the false-positive bar this was
        # held to before landing here.
        if: steps.scope.outputs.go_mode != 'skip'
        run: cd server && go run ./tools/cursordeadlock ./internal ./cmd ./pkg

      - name: Run migrations
        if: steps.scope.outputs.go_mode != 'skip'
        run: cd server && go run ./cmd/migrate up

      # Isolated database for internal/researchrun. Its migration down/up
      # replays must not touch the schema the other integration packages share.
      - name: Prepare isolated research integration database
        if: steps.scope.outputs.go_mode != 'skip' && (steps.go_pkgs.outputs.packages == './...' || contains(steps.go_pkgs.outputs.packages, './internal/researchrun'))
        env:
          PGPASSWORD: multica
        run: |
          psql -h localhost -p 55432 -U multica -d postgres -v ON_ERROR_STOP=1 \
            -c 'CREATE DATABASE multica_research;'
          cd server && DATABASE_URL="$TEST_DATABASE_URL" go run ./cmd/migrate up

      - name: Test
        # Integration packages share this job's Postgres service. Run packages
        # serially so a global scheduler repair cannot mutate another package's
        # temporary workspace while its fixture is still being assembled.
        if: steps.scope.outputs.go_mode != 'skip'
        run: cd server && go test -p 1 ${{ steps.go_pkgs.outputs.packages }}

      - name: Skip unaffected backend tests
        if: steps.scope.outputs.go_mode == 'skip'
        run: echo "No server packages in this PR; backend tests skipped"

      # LRM-1076 AC1/AC5 lives in the main `go test ./...` suite above
      # (TestProcessResearchNextSteps_SilentWindowAutoStepsGe3). The old
      # dedicated evidence step required LRM_1076_EVIDENCE_PATH, which #2885
      # stopped writing — keep assertions there, not a brittle file check here.
