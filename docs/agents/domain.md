# Domain Docs

This repository uses a single domain context.

Before exploring or changing a domain:

- Read the root `CONTEXT.md`.
- Read relevant decisions under `docs/adr/`.
- Use canonical glossary terms and avoid listed synonyms.
- Surface conflicts with an ADR instead of silently overriding it.
- Create or update domain terms and ADRs through `domain-modeling`.
