# Domain Docs

Before exploring or changing a domain:

- Read relevant rules in `docs/engineering-principles.md` and decisions under
  `docs/adr/`.
- Surface conflicts with an existing rule or ADR instead of silently
  overriding it.
- Record new durable rules in `docs/engineering-principles.md` and architectural
  decisions under `docs/adr/`.
