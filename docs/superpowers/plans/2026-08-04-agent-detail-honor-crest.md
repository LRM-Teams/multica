# Agent 详情战力卡荣誉徽章修复记录

## 目标

- Agent 详情页的“舰队战力”卡展示当前荣誉等级徽章，不再展示旧舰级图形。
- 舰级名称与前三名次继续作为文字状态显示。
- 共享舰级标签不再夹带旧舰级图形，避免荣誉页和侧栏摘要继续遗漏。

## 记录

- [x] 根据用户截图定位到 `AgentDetailOverview/FleetHonorCard`。
- [x] 查明卡片直接使用 `FleetClassIcon`，`FleetRankBadge` 内部又重复使用一次。
- [x] 审计 `FleetRankBadge` 全部调用：仅用于 Agent 详情、荣誉页与荣誉侧栏；头像右上角的舰队名次数字为独立组件，不属于旧图形。
- [x] 增加回归测试；旧实现因缺少等级徽章而按预期失败。
- [x] 替换卡片视觉，删除空心装饰圆与白色图标底座，并删除共享舰级标签中的旧图形。
- [x] 目标测试 5/5 通过；`@multica/ui`、`@multica/views` 类型检查通过；改动文件 ESLint 通过；React Doctor 扫描 5 个文件、0 个问题。
- [x] 运行 `make check`：TypeScript 类型检查通过，前端 4,149 个测试通过，Go 全量测试通过；E2E 未通过，失败包含旧 fixture 写入已删除的 `agent.visibility` 列，以及冷启动并发编译期间的大面积登录/导航超时。本次未修改 E2E、认证、数据库或路由代码，因此不把完整校验记为通过。
- [x] 处理计划外本地问题：确认同一工作树内运行近 6 小时的旧 Next 进程已无响应，只终止该工作树的旧进程并让检查脚本启动新前端；该问题不属于用户产品交互。
- [x] 检查最终差异：Agent 业务页面已无 `FleetClassIcon` 调用；三处舰级标签共用无旧图形的 `FleetRankBadge`；改动未触及认证、路由、API 或数据库。
- [x] 提交并创建目标为 `dev` 的非草稿 PR：#2233。
