# 智能体荣誉后台表单可访问性记录

日期：2026-08-04

目标：让屏幕阅读器能够识别智能体荣誉后台中每个成就规则与人工修正控件的用途。

## 执行记录

- [x] 审计智能体荣誉后台的全部表单控件，而非只修改扫描器指出的两处。
- [x] 确认成就启用开关、成就目标值、修正类型、经验调整值、成就选择和修正原因均缺少关联名称。
- [x] 为六类控件增加按当前语言生成的 `aria-label`；成就相关标签包含具体成就名称。
- [x] 四种语言同步增加标签文案，语言包 parity 保持一致。
- [x] 将后台内容组件的 `agent` 输入收窄为实际使用的 `id`，便于独立测试且不创造伪造完整 Agent 对象。
- [x] 新增角色查询测试，验证所有控件可按可访问名称找到，并覆盖“经验调整”与“成就发放”两种状态。
- [x] `pnpm --filter @multica/views typecheck` 通过。
- [x] 后台表单与语言包 parity 测试：2 个文件、189 条通过。
- [x] 变更文件 ESLint 与 `git diff --check` 通过。
- [x] React Doctor 以 `origin/dev` 为基线扫描 2 个变更源文件；此前的控件缺少名称警告已消失，剩余 6 条为该大文件既有维护性提示。

## 中文可访问名称

- `启用星海首航`
- `星海首航目标值`
- `修正类型`
- `经验调整值`
- `选择成就`
- `修正原因`

## 提交

- [x] 非草稿 PR：[#2151](https://github.com/LRM-Teams/multica/pull/2151)，目标分支 `dev`。
- [x] PR 提交后不代替用户合并。
