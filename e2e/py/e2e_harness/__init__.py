"""Env-dispatch issue E2E harness package.

Importable helpers (config, auth, HTTP client, sandbox exec, chain
orchestration) so unit tests can mock the HTTP layer. See
specs/002-env-dispatch-issue-e2e/plan.md.
"""
