// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: agent.sql (ListAgentIDsArchivedBefore)
//
// Hand-aligned while make sqlc is broken repo-wide (task #83). Cross-checked
// against the query text in server/pkg/db/queries/agent.sql — verified by
// running a full `sqlc generate` in an isolated worktree and copying this
// query's output byte-for-byte; the full regen also touches ~4300 unrelated
// lines across 23 files from pre-existing drift (task #85) and must not be
// committed wholesale.

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const listAgentIDsArchivedBefore = `-- name: ListAgentIDsArchivedBefore :many
SELECT id, archived_at
FROM agent
WHERE workspace_id = $1 AND id = ANY($2::uuid[]) AND archived_at IS NOT NULL AND archived_at < $3
`

type ListAgentIDsArchivedBeforeParams struct {
	WorkspaceID pgtype.UUID        `json:"workspace_id"`
	AgentIds    []pgtype.UUID      `json:"agent_ids"`
	Before      pgtype.Timestamptz `json:"before"`
}

type ListAgentIDsArchivedBeforeRow struct {
	ID         pgtype.UUID        `json:"id"`
	ArchivedAt pgtype.Timestamptz `json:"archived_at"`
}

func (q *Queries) ListAgentIDsArchivedBefore(ctx context.Context, arg ListAgentIDsArchivedBeforeParams) ([]ListAgentIDsArchivedBeforeRow, error) {
	rows, err := q.db.Query(ctx, listAgentIDsArchivedBefore, arg.WorkspaceID, arg.AgentIds, arg.Before)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListAgentIDsArchivedBeforeRow{}
	for rows.Next() {
		var i ListAgentIDsArchivedBeforeRow
		if err := rows.Scan(&i.ID, &i.ArchivedAt); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
