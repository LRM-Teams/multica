/** LRM-1348 harness stub — no live projection, so rows fall back to the
 *  status-derived verb exactly like a cold channel load does. */
export function useAgentActivityProjection() {
  return null;
}
