# Research Fleet source map

| Claim | Source |
| --- | --- |
| HTTP routes under `/api/research` | `server/cmd/server/router.go` Research Fleet block |
| Durable Run HTTP, reversible user Stop, V6 archive-on-delete, and task-bound result authorization | `server/internal/handler/research_run_http.go`, `server/internal/handler/research_ops.go`, `server/internal/researchrun/postgres_tasks.go`, `server/cmd/server/router.go` |
| Run scheduler, leases, retries, recovery, typed remediation routing, queued-delivery expiry recovery, bounded evaluation feedback, ranked question frontier, canonical information gain, marginal-gain and delivery decisions | `server/internal/researchrun/engine.go`, `store.go`, `failure_policy.go`, `postgres_tasks.go`, `postgres_gate.go`, `postgres_gate_v4.go`, `information_gain.go`, `server/internal/scheduler/jobs_research_run.go` |
| Canonical task/progress/evidence/event ledgers | migrations `274_research_run_backend`, `276_research_report_quality`, `284_research_evidence_fitness`; `server/internal/researchrun/postgres*.go` |
| Current contract and accepted Research Method read model, plus validated steering limits | `server/internal/researchrun/config.go`, `postgres.go`, `postgres_result.go`, `postgres_tasks.go` |
| Strict structured result envelope, pinned V1–V4 behavior, V4+ Method/evidence-standard and executable delivery-path contract, V5 structured evaluation defects, and Claim-level evidence-fitness Gate | `server/internal/researchrun/result.go`, `result_v2.go`, `result_v3.go`, `result_v4.go`, `result_v5.go`, `result_test.go`, `result_v5_test.go`, `postgres_result.go`, `postgres_gate.go`, `postgres_gate_v4.go` |
| Inbox dispatch idempotency, frozen Manifest ID/hash context binding, Research-owned runtime-recovery retry policy, and event projection | `server/internal/researchrun/dispatch_request.go`, `server/internal/researchrun/artifact_dispatch_prompt.go`, `server/internal/handler/research_run_adapter.go`, `server/internal/service/task.go`, `server/internal/service/task_test.go` |
| Legacy authoritative mutations rejected for initialized runs | `server/internal/handler/research_run_guard.go` plus guarded handlers in `research_ops.go` and `research_product_rounds.go` |
| Fleet ensure + seed roles, fixed per-Run Report Boss membership, reporter-only task-bound report-design skill delivery, and Report Boss identity healing | `server/internal/handler/research_fleet.go`, `research_templates.go`, `research.go`, `onboarding_agent_capability.go`, `agent_inbox.go`; `server/internal/researchrun/postgres_bootstrap_v6.go`; migration `459_research_v6_report_boss`; `server/internal/service/builtin_skills.go` |
| Dynamic roster hire/optimize/archive (lead-only, cap, audit) | `server/internal/handler/research_ops.go`, `research_roster.go` |
| Legacy graph/source/report/message/stage/handoff for uninitialized sessions | `server/internal/handler/research_ops.go`, `research_stage.go`, `research_handoff.go` |
| Session kickoff graph + process cards | `server/internal/handler/research_kickoff.go`, `research_process.go` |
| Message `card_kind` / `meta` | migration `246_research_message_cards` |
| Unique active fleet role + dedupe | migration `247_research_fleet_member_role_unique` |
| Mirror agent chat replies into research drawer; V6 rejects pure-English runner narration at this final user-visible boundary and substitutes a Chinese status summary | `server/internal/service/research_chat_mirror.go` |
| Session wake / fleet dispatch + archived wake gate | `server/internal/handler/research_dispatch.go`, `server/internal/researchwake` |
| Domain playbooks | `references/playbooks/*.md` + `seedResearchFleetPlaybooks` |
| Schema | `server/migrations/244_research_fleet.up.sql` |
| SQL | `server/pkg/db/queries/research.sql` |
| WS events | `server/pkg/protocol/research_events.go`; `server/internal/handler/research_run_adapter.go` keeps legacy graph broadcasts and emits run-scoped `research_projection_v6:delta` sequence-advance signals (`{run_id, through_sequence}`) from committed Event sequence; `packages/core/research/ws-updaters.ts` and `packages/core/realtime/use-realtime-sync.ts` coalesce those signals into authoritative snapshot Query refreshes while the V6 live controller fetches the per-snapshot `projection_delta` chain over the authenticated HTTP resume route |
| CLI and credential-proxy transport, online Director runtime admission, submission diagnostics, producer-frozen schema validation for full result artifact reads, and terminal Inbox cleanup | `server/cmd/multica/cmd_research.go` (V1–V5 `task-result`; V6 Manifest, Manifest-authorized full Work artifact reads, Director Brief, catalog, acknowledgement, immutable report upload, and strict submission; session snapshot and roster administration); `server/internal/handler/research_v6_readiness.go`; `server/internal/handler/research_v6_work.go`; `server/internal/handler/research_v6_error_test.go`; `server/internal/researchrun/postgres_work_artifact_v6.go`; `server/internal/researchrun/postgres_work_item_recovery.go`; `server/internal/researchrun/work_item_fault_injection_test.go` |
| Frontend | `packages/views/research/`, paths `research` / `researchDetail` |
| Artifact passport (D): policy, manifest freeze, dispatch outbox and Agent Inbox identity binding, acceptance grant/membership fence, result artifact lineage | migrations `318`–`330`, `346`–`349`; `server/internal/researchrun/artifact*.go`, `artifact_context.go`, `artifact_manifest.go`, `artifact_dispatch_prompt.go`, `artifact_representation.go`, `artifact_taint.go`, `dispatch_request.go`, `postgres_artifact.go`, `postgres_tasks.go`, `postgres_result.go`, `postgres_evidence.go`; `server/internal/handler/research_run_adapter.go`; Contract/Method/Question/Task/Attempt and Source Snapshot/Observation/Claim reads decode frozen ordered bytes before content access; Quality Gate/Citation Audit use `evaluation` purpose with separate normal + evaluation-private grants and only their assigned grader Prompt receives frozen Stage Evaluation context; manifest hash binds scope/purpose/watermarks/representations and is carried with its ID through the immutable request into Agent Inbox context; dispatch/replay use frozen principal header; acceptance re-locks both exact grants and active Fleet membership before commit; accepted Result and materialized outputs inherit the most sensitive manifest access level, and unsafe reuse fails closed; assigned Agent snapshot requires `--attempt-id`; unbound active Fleet members receive only bounded session status/stage plus Fleet preview from `server/internal/handler/agent_research.go`; `packages/core/research/schemas.ts` parses bounded `attempt_context` |
| Legacy Inquiry Graph provenance | migrations `348_research_inquiry_graph`, `350_research_inquiry_graph_guards`; superseded for V6 by ADR-0017 and retained only for V1–V5 compatibility |
| Auditable source-screening contract (F, persistence wiring pending) | `server/internal/researchrun/screening_policy.go`, `screening_policy_test.go`; versioned inclusion/exclusion criteria, disposition matrix, reviewer identity, located facts, canonical duplicate identity and stable decision fingerprint |
| Director V6 contract and strict nine-envelope validation (user-facing V6 create is open) | `docs/contracts/research-run-v6.schema.json`, `docs/contracts/research-run-v6-director.schema.json`, `docs/research-run-v6-contract.md`, `server/internal/researchrun/result_v6*.go`, `v6_contract_test.go`, `v6_contract_hash_test.go`; omitted `orchestrator_version` still defaults to V5; explicit V6 + Director creates a V6 Run; `AssessV6Activation` remains an audit and does not flip that omitted-version default |
| Director, dynamic team, Work Items, graph versions/absorption, discussions, steering, reports, recovery, user-explainable Work/assignment projection (operational `director` cycle Work is excluded from every canvas projection slice; visual territories resolve leaf Branches to their top-level child below the canonical root), exact Attempt+Inbox-task persisted activity presentation, the `director` Run Event actor, and one-time recovery of invalid autonomous Director pauses are durable PostgreSQL facts | migrations `390`–`408`, `427_research_v6_director_event_actor`, `440_research_v6_director_pause_recovery`, `443_research_v6_work_activity`; `server/internal/researchrun/postgres_*_v6.go`, `postgres_director*.go`, `postgres_work_item*.go`, `node_v6*.go`, `discussion_v6.go`, `steering_v6.go`, `report_v6.go`, `projection_v6*.go`, `work_activity_v6.go`, `canonical_state.go`; `server/internal/activityprojection/projection.go`; `server/internal/handler/research_v6_work_activity.go`, `agent_inbox.go` |
| V6 HTTP, realtime, graph clients, report isolation, and activation operations | `server/internal/handler/research_v6_*.go`, `research_report_v6.go`, `research_report_document.go`, `research_run_adapter.go`; `packages/core/research/`; `packages/views/research/` including `research-v6-report-sandbox.ts`; the shared view is mounted by Web/Desktop; `server/internal/researchrun/v6_activation.go`; `server/internal/metrics/business_sampler*.go`; `deploy/helm/multica/templates/prometheusrule.yaml` |
| V6 product close: Director message routing, home-list Work Item progress, release rollback, Monitor, production Episode/window, four-kind Source persist, rebuildable events | `server/internal/handler/research_message_target.go`, `research_list_v6.go`, `research_v6_release.go`, `research_v6_monitor.go`, `research_v6_eval.go`, `research_v6_source.go`; `server/internal/researchrun/source_persist.go`, `event_rebuild.go`; migration `426_research_v6_product_close`; scheduler `jobs_research_monitor.go`, `jobs_research_eval.go`; `packages/views/research/components/research-home-overview.tsx`, `research-v6-ops-panel.tsx` |
| V6 executable Agent dispatch prompt (exact atomic Work mapping; exactly one non-root child Branch per atomic Work; mandatory same-tier Frontier convergence; exact `create_integration`/`integration.create.v1` convergence mapping; frozen Steward Discussion fan-out; consensus-triggered Integration Work; executable discussion and integration envelopes; S→M→L→XL→XXL promotion, assimilation and XXL merge rules; server-selected one-maximum-per-direction Report Boss inputs, title-only Director report intent, transaction-owned reporter/input/event snapshot, draft Report passport shells and non-lineage creation events, single-flight report revisions, and report-maintenance liveness alone or with partial valid follow-up Work; Director self-assignment rejection, one-active-Work-per-Agent admission and idle-only dispatch, minimum first-round parallelism, overlap-safe escalated-Discussion/open-question coverage, joined-worker-safe mixed staffing and dispatch, current-Run reference preflight, contract-rejection/failed-worker recovery, Simplified-Chinese narration, exact Branch refs and Manifest-authorized fallback endpoints), Director Brief freshness fence, immediate identity-scoped Director proposal settlement, strict-boundary rejection settlement and stale-Manifest cycle replacement with scheduler restart recovery, atomic Work/Task provenance, durable credential-proxy attempt fence, poison-item quarantine, outbox recovery, and lost-attempt Inbox cleanup | `server/internal/researchrun/director_prompt_v6.go`, `work_prompt_v6.go`, `work_submission_v6.go`, `postgres_work_submission_v6.go`, `postgres_director_action_v6.go`, `director_actions_integration_v6.go`, `director_proposal_preflight_v6.go`, `report_input_selector_v6.go`, `artifact_report.go`, `postgres_report_work_v6.go`, `postgres_report_package_v6.go`, `postgres_director_brief.go`, `postgres_discussion_v6.go`, `event_trigger_v6.go`, `director_actions_core_v6.go`, `director_actions_lifecycle_v6.go`, `v6_backing_task.go`, `postgres_work_dispatch_v6.go`, `postgres_outbox_v6.go`, `postgres_work_item_recovery.go`; `server/internal/handler/research_run_adapter.go`, `research_v6_runtime_adapter.go`, `research_v6_work.go` |
| V6 live progress notes (`POST ${V6_API}/progress` → `v6_work_progress_reported` operational Run Event, pure-English narration replaced by a bounded Chinese status, per-attempt budget, active-attempt fence, sliding Work Item lease heartbeat), immediate `research_session:presence` broadcast, Work detail reads from the same V6 event, and V6 run presence derived from the run-scoped team roster + latest Work Item + agent-authored caption events | `server/internal/researchrun/work_progress_v6.go`, `postgres_work_progress_v6.go`, `work_activity_v6.go`, `work_prompt_v6.go`; `server/internal/handler/research_v6_work.go`, `research_v6_work_activity.go`, `research_presence_v6.go`, `research_ops.go`; `packages/views/research/v6-session-adapter/use-research-v6-work-activity.ts` |
| V6 stall self-healing: 45-minute dispatch lease, Director retry/reassign resets the attempt budget, budget-exhausted 'ready' zombies fail closed, retryable model/runtime exhaustion reopens once after bounded backoff, terminal Attempts release idle membership, state-racing or idempotently replayed event triggers cannot head-of-line block unrelated Runs/events, and idle running Runs wake through a capped Director catch-up cycle (`idle:<sequence>` trigger) | `server/internal/researchrun/postgres_work_dispatch_v6.go`, `director_actions_lifecycle_v6.go`, `postgres_work_item_recovery.go`, `event_trigger_v6.go`, `engine.go` |
| Offline evaluation subject boundary (M2a) | `server/internal/researcheval/subject.go`; seals only Task + controlled Environment + seed, rejects Oracle/unknown fields and content/hash drift; Passport/Manifest and production Executor wiring remain follow-up work |
