package testagent

// Model is the explicit model identifier used by DB-backed integration tests.
// Production agents must not use provider-specific sentinels such as "auto".
const Model = "composer-1.5"
